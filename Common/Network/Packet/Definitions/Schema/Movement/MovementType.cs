using System;
namespace Common.Network.Packet.Definitions.Schema.Movement
{
    public enum MovementType : int
    {
        UP = 0,
        DOWN = 1,
        LEFT = 2,
        RIGHT = 3,
        STOPPED = 4
    }
}
